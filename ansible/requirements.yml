---
collections:
  - name: community.general
    version: ">=10.0.0"
  - name: ansible.posix
    version: ">=2.0.0"
  # OpenWrt management via pure-shell (ash) modules: no Python needed on the
  # router. Used by roles/openwrt-iot-vlan. Requires ansible-core >= 2.18 on the
  # control node (the in-cluster ansible-runner image must be bumped before it
  # can drift-check the openwrt host; workstation applies are fine).
  - name: community.openwrt
    version: ">=1.0.0"
