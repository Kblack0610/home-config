---
galaxy_info:
  role_name: ollama
  author: home-config
  description: >-
    Install Ollama via Homebrew on macOS and manage it as a brew service.
    Ollama is the fallback LLM inference path on mac-studio; the primary
    path is MLX (see launchd-mlx-services role).
  license: MIT
  min_ansible_version: "2.14"
  platforms:
    - name: MacOSX
      versions:
        - all
  galaxy_tags:
    - macos
    - llm
    - ollama
dependencies: []
