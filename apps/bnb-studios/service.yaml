apiVersion: v1
kind: Service
metadata:
  name: bnb-studios
  labels:
    app: bnb-studios-web
spec:
  selector:
    app: bnb-studios-web
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      name: http
  type: ClusterIP
