apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: cal

resources:
  - namespace.yaml
  - secret.yaml
  - configmap.yaml
  - pvc.yaml
  - web-deployment.yaml
  - web-service.yaml
  - ingress.yaml
