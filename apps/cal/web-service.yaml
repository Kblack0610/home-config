apiVersion: v1
kind: Service
metadata:
  name: cal-web
  namespace: cal
  labels:
    app.kubernetes.io/name: cal
    app.kubernetes.io/component: web
spec:
  selector:
    app.kubernetes.io/name: cal
    app.kubernetes.io/component: web
  ports:
    - port: 80
      targetPort: 3000
  type: ClusterIP
