apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: fleet

resources:
  - namespace.yaml
  - secret.sops.yaml
  # datastores (kept off asus-laptop via the fleet.storage/node label)
  - mysql-pvc.yaml
  - mysql-deployment.yaml
  - mysql-service.yaml
  - redis-deployment.yaml
  - redis-service.yaml
  # server
  - deployment.yaml
  - service.yaml
  - ingress.yaml
