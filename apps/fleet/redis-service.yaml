apiVersion: v1
kind: Service
metadata:
  name: fleet-redis
  namespace: fleet
  labels:
    app.kubernetes.io/name: fleet
    app.kubernetes.io/component: redis
spec:
  selector:
    app.kubernetes.io/name: fleet
    app.kubernetes.io/component: redis
  ports:
    - port: 6379
      targetPort: 6379
  type: ClusterIP
