title: Homelab
views:
  # Overview (glance) leads the tab bar; Devices is second — matching the wall.
  - !include dashboards/overview.yaml
  - !include dashboards/devices.yaml
  - !include dashboards/launcher.yaml
  - !include dashboards/local.yaml
  - !include dashboards/ops.yaml
  - !include dashboards/admin.yaml
  # Hidden device-detail subviews (subview: true → not in the tab bar; reached
  # from the Devices hub). Shared with the wall dashboard via the same files.
  - !include dashboards/device_vacuum.yaml
  - !include dashboards/device_litterbox.yaml
  - !include dashboards/device_bambu.yaml
  - !include dashboards/device_neptune.yaml
  - !include dashboards/device_speakers.yaml
