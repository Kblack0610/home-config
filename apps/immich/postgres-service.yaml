apiVersion: v1
kind: Service
metadata:
  name: immich-postgres
  namespace: immich
  labels:
    app.kubernetes.io/name: immich
    app.kubernetes.io/component: database
spec:
  selector:
    app.kubernetes.io/name: immich
    app.kubernetes.io/component: database
  ports:
    - port: 5432
      targetPort: 5432
  type: ClusterIP
