apiVersion: v1
kind: Service
metadata:
  name: intel-gpu-exporter
  namespace: intel-gpu-exporter
  labels:
    app.kubernetes.io/name: intel-gpu-exporter
spec:
  selector:
    app.kubernetes.io/name: intel-gpu-exporter
  ports:
    - name: metrics
      port: 9101
      targetPort: 9101
