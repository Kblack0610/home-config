apiVersion: v1
kind: Service
metadata:
  name: karakeep-chrome
  namespace: karakeep
  labels:
    app.kubernetes.io/name: karakeep
    app.kubernetes.io/component: chrome
spec:
  selector:
    app.kubernetes.io/name: karakeep
    app.kubernetes.io/component: chrome
  ports:
    - port: 9222
      targetPort: 9222
  type: ClusterIP
