apiVersion: v1
kind: Service
metadata:
  name: karakeep-meilisearch
  namespace: karakeep
  labels:
    app.kubernetes.io/name: karakeep
    app.kubernetes.io/component: meilisearch
spec:
  selector:
    app.kubernetes.io/name: karakeep
    app.kubernetes.io/component: meilisearch
  ports:
    - port: 7700
      targetPort: 7700
  type: ClusterIP
