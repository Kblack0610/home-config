apiVersion: v1
kind: Service
metadata:
  name: karakeep-web
  namespace: karakeep
  labels:
    app.kubernetes.io/name: karakeep
    app.kubernetes.io/component: web
spec:
  selector:
    app.kubernetes.io/name: karakeep
    app.kubernetes.io/component: web
  ports:
    - port: 80
      targetPort: 3000
  type: ClusterIP
