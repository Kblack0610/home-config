apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mem0-history
  namespace: memory
  labels:
    app.kubernetes.io/name: mem0
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 1Gi
