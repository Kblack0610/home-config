apiVersion: v1
kind: Service
metadata:
  name: mem0
  namespace: memory
  labels:
    app.kubernetes.io/name: mem0
spec:
  selector:
    app.kubernetes.io/name: mem0
  ports:
    - name: http
      port: 8000
      targetPort: 8000
  type: ClusterIP
