apiVersion: v1
kind: Service
metadata:
  name: notes-sync-bridge
  namespace: notes-sync-bridge
  labels:
    app.kubernetes.io/name: notes-sync-bridge
spec:
  selector:
    app.kubernetes.io/name: notes-sync-bridge
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
