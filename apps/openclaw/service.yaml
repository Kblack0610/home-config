apiVersion: v1
kind: Service
metadata:
  name: openclaw
  namespace: openclaw
  labels:
    app.kubernetes.io/name: openclaw
spec:
  selector:
    app.kubernetes.io/name: openclaw
  ports:
    - name: http
      port: 18789
      targetPort: 18789
  type: ClusterIP

