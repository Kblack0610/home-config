apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Co-located in the home-assistant namespace — HA is the only consumer, and the
# Wyoming service DNS becomes wyoming-openwakeword.home-assistant.svc.cluster.local
namespace: home-assistant

resources:
  - deployment.yaml
  - service.yaml
