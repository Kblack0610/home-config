apiVersion: v1
kind: Service
metadata:
  name: wyoming-openwakeword
  namespace: home-assistant
  labels:
    app.kubernetes.io/name: wyoming-openwakeword
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: wyoming-openwakeword
  ports:
    - name: wyoming
      port: 10400
      targetPort: 10400
      protocol: TCP
