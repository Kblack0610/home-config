apiVersion: v1
kind: Service
metadata:
  name: orcaslicer
  namespace: orcaslicer
  labels:
    app.kubernetes.io/name: orcaslicer
spec:
  selector:
    app.kubernetes.io/name: orcaslicer
  ports:
    - port: 80
      targetPort: 3000
  type: ClusterIP
