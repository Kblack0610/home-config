apiVersion: v1
kind: Service
metadata:
  name: print-service
  namespace: orcaslicer
  labels:
    app.kubernetes.io/name: print-service
spec:
  selector:
    app.kubernetes.io/name: print-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
