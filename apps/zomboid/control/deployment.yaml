apiVersion: apps/v1
kind: Deployment
metadata:
  name: zomboid-control
  namespace: zomboid
  labels:
    app.kubernetes.io/name: zomboid-control
spec:
  # Unlike the game server this stays up: it is what wakes the game server, so it
  # cannot itself be asleep. It costs ~32Mi.
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: zomboid-control
  template:
    metadata:
      labels:
        app.kubernetes.io/name: zomboid-control
    spec:
      serviceAccountName: zomboid-control
      securityContext:
        runAsNonRoot: true
        runAsUser: 65534
        runAsGroup: 65534
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: control
          image: python:3.12-slim
          command: ["python3", "/app/server.py"]
          env:
            - name: NAMESPACE
              value: zomboid
            - name: DEPLOYMENT
              value: zomboid
            - name: CONTROL_TOKEN
              valueFrom:
                secretKeyRef:
                  name: zomboid-control-token
                  key: control-token
          ports:
            - name: http
              containerPort: 8080
          volumeMounts:
            - name: app
              mountPath: /app
              readOnly: true
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          resources:
            requests:
              memory: "32Mi"
              cpu: "10m"
            limits:
              memory: "128Mi"
              cpu: "200m"
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8080
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8080
            periodSeconds: 10
      volumes:
        - name: app
          configMap:
            name: zomboid-control-app
